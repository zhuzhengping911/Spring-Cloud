server:
  port: 91111
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:1111/eureka/
spring:
  application:
    name: zipkin-service
